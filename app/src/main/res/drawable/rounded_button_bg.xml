<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Solid royal blue for clean modern look -->
    <solid android:color="@color/primary" />

    <!-- Rounded corners -->
    <corners android:radius="14dp"/>

    <!-- Inner padding feel -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="6dp"
        android:bottom="6dp"/>

</shape>